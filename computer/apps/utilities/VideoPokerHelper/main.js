var deck; var hand; function get5CardsToKeep() { "use strict"; if (hand.isRoyalFlush(hand) || hand.isStraightFlush(hand) || hand.isFourOfAKind(hand) || hand.isFullHouse(hand) || hand.isFlush(hand) || hand.isStraight(hand)) { console.log("get5CardsToKeep() = " + hand.cards); return hand.cards; } console.log("get5CardsToKeep() = null"); return null; } function isStraightDraw(cards) { "use strict"; if ((cards[cards.length - 1].order - cards[0].order) <= 5) { return true; } if ((cards[0].rank == "2") && (cards[cards.length - 1].rank == "A") && ((cards[cards.length - 2].order - cards[0].order) <= 5)) { return true; } return false; } function getTwoPair() { "use strict"; var groups = hand.groupCardsByRank(); var pairsReturned = []; for ( var rank in groups) { if (groups[rank].length == 2) { pairsReturned.push(groups[rank][0]); pairsReturned.push(groups[rank][1]); } } console.log("pairsReturned() = " + pairsReturned); return pairsReturned; } function get4CardsToKeep() { "use strict"; var groups; var straightReturned = []; var unmatchedCard; var pairs; if (hand.isTwoPair()) { pairs = getTwoPair(); console.log("get4CardsToKeep() = " + pairs); return pairs; } groups = hand.groupCardsBySuit(); for ( var suit in groups) { if(groups.hasOwnProperty(suit)) { if (groups[suit].length == 1) { unmatchedCard = groups[suit][0]; } if (groups[suit].length == 4) { groups[suit].sort(function (card1, card2) { return card1.order - card2.order; }); if (isStraightDraw(groups[suit])) { straightReturned.push(groups[suit][0]); straightReturned.push(groups[suit][1]); straightReturned.push(groups[suit][2]); straightReturned.push(groups[suit][3]); } } } } if (straightReturned.length == 4) { console.log("straightReturned = " + straightReturned); console.log("straightReturned[3].order = " + straightReturned[3].order); console.log("unmatchedCard = " + unmatchedCard); console.log("unmatchedCard.order = " + unmatchedCard.order); if ((straightReturned[3].order < 11) && (unmatchedCard.order > 10)) { console.log("get4CardsToKeep() = " + [ unmatchedCard ]); return [ unmatchedCard ]; } else { console.log("get4CardsToKeep() = " + straightReturned); return straightReturned; } } console.log("get4CardsToKeep() = null"); return null; } function get3CardsToKeep() { "use strict"; var groups = hand.groupCardsByRank(); for ( var rank in groups) { if (groups[rank].length == 3) { console.log("get3CardsToKeep() = " + groups[rank]); return groups[rank]; } } console.log("get3CardsToKeep() = null"); return null; } function findBest2Of3FaceCards(cards) { "use strict"; var groups = {}; var cardsToReturn = []; for (var i = 0; i < cards.length; i += 1) { if (!(cards[i].suit in groups)) { groups[cards[i].suit] = []; } groups[cards[i].suit].push(cards[i]); } for ( var suit in groups) { if (groups[suit].length == 2) { return groups[suit]; } } cardsToReturn[0] = cards[0]; cardsToReturn[1] = cards[1]; return cardsToReturn; } function get2CardsToKeep() { "use strict"; var groups = hand.groupCardsByRank(); var rankOfLowPair = ""; var faceCardsFound = []; var best2Of3Cards; for ( var rank in groups) { if (groups[rank].length == 2) { if ((rank == "J") || (rank == "Q") || (rank == "K") || (rank == "A")) { console.log("get2CardsToKeep() = " + groups[rank]); return groups[rank]; } else { rankOfLowPair = rank; } } else { if ((rank == "J") || (rank == "Q") || (rank == "K") || (rank == "A")) { faceCardsFound.push(groups[rank][0]); } } } if ((rankOfLowPair !== "") && (faceCardsFound.length === 0)) { console.log("get2CardsToKeep() = " + groups[rankOfLowPair]); return groups[rankOfLowPair]; } switch (faceCardsFound.length) { case 0: console.log("get2CardsToKeep() = null"); return null; case 1: case 2: console.log("get2CardsToKeep() = " + faceCardsFound); return faceCardsFound; case 3: best2Of3Cards = findBest2Of3FaceCards(faceCardsFound); console.log("get2CardsToKeep() = " + best2Of3Cards); return best2Of3Cards; } return null; } function getCardsToKeep() { "use strict"; var cards; if ((cards = get5CardsToKeep()) !== null) { return cards; } else if ((cards = get4CardsToKeep()) !== null) { return cards; } else if ((cards = get3CardsToKeep()) !== null) { return cards; } else if ((cards = get2CardsToKeep()) !== null) { return cards; } return []; } function clearCardsToKeep() { "use strict"; var keptSpanElement; var textNode; for (var i = 0; i < 5; i += 1) { keptSpanElement = document.getElementById("kept" + (i + 1)); textNode = keptSpanElement.firstChild; if (textNode) { keptSpanElement.removeChild(textNode); } } } function showCardsToKeep(cardsToKeep) { "use strict"; var keptSpanElement; setupHand(); for (var i = 0; i < cardsToKeep.length; i += 1) { keptSpanElement = document.getElementById("kept" + (hand.cards.indexOf(cardsToKeep[i]) + 1)); keptSpanElement.appendChild(document.createTextNode("KEEP")); } } function findRadioButton(namePrefix, index) { "use strict"; var radios = document.getElementsByName(namePrefix + (index + 1)); for (var i = 0, iMax = radios.length; i < iMax; i += 1) { return radios[i].value; } } function setupHand() { "use strict"; var rank, suit; var card, cards = []; for (var i = 0; i < 5; i += 1) { rank = findRadioButton('r', i); suit = findRadioButton('s', i); card = deck.findCardInDeck(rank, suit); cards.push(card); } hand = new Hand(cards); } function calculate(event) { "use strict"; console.log("entering calculate(" + event + ")"); try { setupHand(); hand.sort(); clearCardsToKeep(); showCardsToKeep(getCardsToKeep()); } catch (exception) { console.log(exception); } console.log("exiting calculate()"); event.preventDefault(); return false; } function init() { "use strict"; var submitButtonElement = document.getElementById("submitButton"); var resetButtonElement = document.getElementById("resetButton"); deck = new Deck(); submitButtonElement.addEventListener("click", calculate); resetButtonElement.addEventListener("click", clearCardsToKeep); } window.addEventListener("load", init); 